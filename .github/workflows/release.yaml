# WARNING: This file is managed automatically and may be overwritten at any time.

name: Release

on:
  push:
    branches: [main, master]

permissions: {}

jobs:
  Release:
    uses: schubergphilis-ep/github-terraform-workflows/.github/workflows/module-release.yaml@4dc5e4c99aea53bb4b5db42bc79ddc28d41b03d9 # v2.3.0
    permissions:
      contents: write
      issues: write
      pull-requests: write
    secrets: inherit
